{
  "id": "4217f745",
  "name": "navigate-to-wikipediaorg.spec.ts",
  "description": "Navigate to wikipedia.org, search for \"Playwright automation\" using the search box, click the first search result, verif",
  "code": "import { test, expect } from '@playwright/test';\n\nconst SF_HOME = 'https://www.wikipedia.org';\n\n// [AI-SYNTHESIZED] CANDIDATES array for demonstration; not used in this test but included per template\nconst CANDIDATES = ['Playwright automation'];\n\ntest.describe('Wikipedia Search and Article Verification', () => {\n  test('Searches for Playwright automation and verifies article page', async ({ page }) => {\n    // [FIXED] Removed login function - Wikipedia doesn't require authentication\n    // This was causing frame context mismatch and \"Locators must belong to same frame\" error\n\n    // Step 1: Navigate to Wikipedia home\n    await page.goto(SF_HOME);\n    await page.waitForLoadState('networkidle');\n    console.log('[step] OK Navigated to Wikipedia home');\n\n    // Step 2: Locate search box and enter query\n    // [AI-HEAL] FIX: Only use page.locator() chains to avoid mixing selector types and frame errors\n    const searchBox = page.locator('input[name=\"search\"]')\n      .or(() => page.locator('input[type=\"search\"]'))\n      .or(() => page.locator('input[placeholder*=\"Search\" i]'));\n    await expect(searchBox.first()).toBeVisible(); // [AI-HEAL] Use .first() to avoid ambiguous multi-match locators\n    await searchBox.first().fill('Playwright automation'); // [AI-HEAL] Use .first() for consistency\n    console.log('[step] OK Filled search box with query');\n\n    // Step 3: Submit search\n    await searchBox.first().press('Enter'); // [AI-HEAL] Use .first() for consistency\n    await page.waitForLoadState('networkidle');\n    console.log('[step] OK Submitted search and waited for results');\n\n    // Step 4: Click the first search result\n    // [AI-HEAL] Add waitForSelector for timing gap before locating first result\n    await page.waitForSelector('main a'); // [AI-HEAL] Wait for at least one link in main content\n    // [AI-HEAL] FIX: Use page.locator('main a').first() to ensure locator is in the same frame\n    const firstResult = page.locator('main a').first(); // [AI-HEAL] Use a single locator chain within the same frame\n    await expect(firstResult).toBeVisible();\n    try {\n      await firstResult.click();\n    } catch (e) {\n      // [AI-HEAL] Fallback click strategy in case of intercepted click\n      await firstResult.evaluate(node => (node as HTMLElement).click());\n    }\n    await page.waitForLoadState('networkidle');\n    console.log('[step] OK Clicked first search result');\n\n    // Step 5: Verify article heading is present\n    // [AI-HEAL] Add waitForSelector for heading to handle dynamic loading\n    await page.waitForSelector('h1'); // [AI-HEAL] Wait for heading to appear\n    const heading = page.locator('h1').first(); // [AI-HEAL] Use page.locator('h1').first() to ensure locator is in the same frame\n    await expect(heading).toBeVisible();\n    console.log('[step] OK Verified article heading is visible');\n\n    // Step 6: Check for Table of Contents section\n    // [AI-HEAL] Use only page.locator() chains for TOC region to avoid frame errors\n    const tocRegion = page.locator('#toc')\n      .or(() => page.locator('nav[aria-label*=\"Contents\" i]'))\n      .or(() => page.locator('[role=\"region\"][aria-label*=\"contents\" i]'))\n      .or(() => page.locator('[role=\"navigation\"][aria-label*=\"contents\" i]'))\n      .or(() => page.locator('h2:has-text(\"Contents\")')); // [AI-HEAL] Use only locator chains to ensure all locators are in the same frame\n    await expect(tocRegion.first()).toBeVisible(); // [AI-HEAL] Use .first() to avoid ambiguity\n    console.log('[step] OK Verified Table of Contents is visible');\n  });\n});",
  "browsers": [
    "msedge"
  ],
  "analysis": {
    "steps": [
      "Navigate to wikipedia.org",
      "Enter 'Playwright automation' in the search box",
      "Submit the search",
      "Click the first search result",
      "Verify the article page loads with a heading",
      "Check the page has a visible table of contents section"
    ],
    "selectors": [
      "Search box with name='search'",
      "First search result anchor link in the results list",
      "Article heading element (e.g., h1)",
      "Table of contents section (e.g., id='toc')"
    ],
    "risks": [
      "Search box selector may change if Wikipedia updates the search form",
      "Results list structure may change, affecting first result selection",
      "Table of contents section may not appear for all articles",
      "Page load timing issues may cause flakiness when verifying heading or TOC"
    ],
    "healingStrategies": [
      "Use role-based or label-based selectors for the search box as fallback",
      "Select the first visible result using text or partial match if structure changes",
      "Add conditional checks for presence of TOC, and log if missing",
      "Use explicit waits for heading and TOC visibility before assertions"
    ]
  },
  "createdAt": "2026-05-27 12:38",
  "healCount": 19,
  "lastHealed": "2026-05-28 09:45",
  "status": "active",
  "steps": 6
}
